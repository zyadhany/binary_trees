#include "binary_trees.h"

/**
 * heap_extract - delette binary tree node.
 * @root: parent node.
 *
 * Return: NULL at error or a pointer to the new node.
 */
int heap_extract(heap_t **root)
{
	if (!root)
		return (0);
	if (!(*root))
		return (0);
	return (1);
}
