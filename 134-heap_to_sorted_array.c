#include "binary_trees.h"

/**
 * heap_to_sorted_array - delette binary tree node.
 * @heap: parent node.
 * @size: a
 *
 * Return: NULL at error or a pointer to the new node.
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	if (!heap)
		return (NULL);
	if (!size)
		return (NULL);
	return (NULL);
}
